# This file is used by Coolify to deploy your application.
# You can find more information about this file here:
# https://coolify.io/docs/deployment/configuration
build:
  - npm i -g pnpm
  - pnpm i
  - pnpm build
start: pnpm start
